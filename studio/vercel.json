{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "cd .. && npm install && npm run build && cd studio && npm install && npm run build",
  "installCommand": "echo 'skip — handled in buildCommand'",
  "framework": "nextjs"
}
